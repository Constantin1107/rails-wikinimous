<div class="m-5">
  <h1>Wikinomous</h1>
  <%= link_to @article.title, article_path(@article.id) , :class => 'text-decoration-none text-dark display-4'%>
  <%= link_to "Delete", article_path(@article),data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete #{@article.title}?"}, :class => 'btn btn-danger'%>
  <p><%= link_to 'Add a new article', new_path , :class => 'btn btn-primary'%></p>
</div>
